# Compute paths
get_filename_component(MINIZIP_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(URIPARSER_INCLUDE_DIR "@CONF_INCLUDE_DIRS@" PARENT_SCOPE)
 
# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET uriparser AND NOT URIPARSER_BINARY_DIR)
  include("${URIPARSER_CMAKE_DIR}/UriParserTargets.cmake")
endif()

# set(LIB_NAMES_ @LIB_NAMES@)
# set(URIPARSER_LIBRARY)
# foreach(LIB_NAME ${LIB_NAMES_}) 
# get_target_property(LIB_LIB_FILENAME ${LIB_NAME} IMPORTED_LOCATION_NOCONFIG)
# set(URIPARSER ${LIB_LIB_FILENAME}  PARENT_SCOPE)
# endforeach()

get_target_property(LIB_LIB_FILENAME @LIB_NAME@ IMPORTED_LOCATION_NOCONFIG)

set(URIPARSER ${LIB_LIB_FILENAME}  PARENT_SCOPE)
set(URIPARSER_FOUND TRUE)
